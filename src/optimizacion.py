# src/optimizacion.py

from deap import base, creator, tools, algorithms
from sklearn.model_selection import train_test_split
from sklearn.metrics import accuracy_score

def optimizar_svm(X, y):
    """Utiliza un algoritmo genético para optimizar los parámetros de SVM."""
    # Configurar el algoritmo genético (similar a lo mostrado anteriormente)
    # ...
    return mejor_modelo
